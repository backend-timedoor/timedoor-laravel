# Golden Examples

Reference implementations of complete features written EXACTLY to standard. Used as fixed benchmarks: when testing the skill, generated code is compared against these.

Add 2-3 small features here (e.g. `product-crud-api/`, `order-refund-service/`) — sanitized, generic domain, each passing the full Definition of Done. Keep them updated when standards change.
